FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (use npm install because lockfile is not tracked)
RUN npm install

# Copy source code
COPY . .

# Set environment for testing
ENV CI=true
ENV NODE_ENV=test

# Run tests
CMD ["npm", "test", "--", "--coverage", "--watchAll=false"]
