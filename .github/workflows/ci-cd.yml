name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

env:
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1
  # Four DISTINCT values on purpose. Sharing one value across scopes is the
  # exact collapse `scripts/cli.py` used to ship: every cross-scope check
  # passes vacuously because the tokens compare equal, so CI would certify a
  # separation it never exercised. Browser Engine no longer falls back to the
  # callback or AI token, so it needs its own or every browser call 401s.
  AI_ENGINE_API_TOKEN: local-test-ai-token
  BACKEND_CALLBACK_TOKEN: local-test-callback-token
  BACKEND_AGENT_API_TOKEN: local-test-agent-token
  BROWSER_ENGINE_API_TOKEN: local-test-browser-token
  PLATFORM_MAINTAINER_SIGNUP_CODE: local-maintainer

jobs:
  # ==================== SECRET SCANNING ====================
  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml

  # ==================== UNIT TESTS ====================
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-unit-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-unit-
            ${{ runner.os }}-buildx-

      - name: Run unit tests
        run: |
          docker compose --profile test-unit up --build --abort-on-container-exit test-unit

      - name: Copy test results
        if: always()
        run: |
          mkdir -p test-results coverage
          CONTAINER_ID=$(docker compose --profile test-unit ps -q test-unit)
          if [ -n "$CONTAINER_ID" ]; then
            docker cp $CONTAINER_ID:/app/test-results/. ./test-results/ || true
            docker cp $CONTAINER_ID:/app/coverage/. ./coverage/ || true
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: test-results/

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-coverage
          path: coverage/

      - name: Cleanup
        if: always()
        run: docker compose --profile test-unit down -v

  # ==================== SECURITY INVARIANT SUITE ====================
  # The frozen black-box suite (tests/security/, SECSUITE_VERSION) is NOT a
  # subset of tests/unit and is not run by test-unit's default command --
  # without this job the suite can be fully broken while unit-tests stays
  # green. Managed mode boots each service in-process via ASGI with a
  # controlled env, so this needs only the test-unit image, not the running
  # stack. See tests/security/MODULE.md for the documented local command.
  security-tests:
    name: Security Invariant Suite
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-security-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-security-
            ${{ runner.os }}-buildx-

      - name: Build test-unit image
        run: |
          docker compose --profile test-unit build test-unit

      - name: Run security-invariant suite
        run: |
          docker compose --profile test-unit run --rm test-unit \
            pytest tests/security -m "security and not integration" -q

      - name: Cleanup
        if: always()
        run: docker compose --profile test-unit down -v

  # ==================== INTEGRATION TESTS ====================
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: unit-tests
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Start test infrastructure
        run: |
          docker compose --profile default --profile test-integration up -d ollama
          sleep 15

      - name: Start services
        run: |
          docker compose --profile default --profile test-integration up -d --build backend-core ai-engine browser-engine

      - name: Wait for services to be healthy
        run: |
          for url in http://localhost:8080/health http://localhost:8090/health http://localhost:8083/health; do
            timeout 120 bash -c "until curl -fsS $url > /dev/null 2>&1; do sleep 2; done"
            echo "ready: $url"
          done

      - name: Build test image
        run: |
          docker compose --profile default --profile test-integration build test-integration

      - name: Run integration tests
        run: |
          docker compose --profile default --profile test-integration run --rm --no-deps test-integration

      - name: Copy test results
        if: always()
        run: |
          mkdir -p test-results
          CONTAINER_ID=$(docker compose --profile default --profile test-integration ps -q test-integration)
          if [ -n "$CONTAINER_ID" ]; then
            docker cp $CONTAINER_ID:/app/test-results/. ./test-results/ || true
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-results
          path: test-results/

      - name: Show service logs
        if: failure()
        run: |
          docker compose --profile default --profile test-integration logs backend-core
          docker compose --profile default --profile test-integration logs ai-engine
          docker compose --profile default --profile test-integration logs browser-engine

      - name: Cleanup
        if: always()
        run: docker compose --profile default --profile test-integration down -v

  # ==================== END-TO-END TESTS ====================
  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    needs: integration-tests
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Start infrastructure
        run: |
          docker compose --profile default --profile test-e2e up -d ollama
          sleep 15

      - name: Start all services
        run: |
          docker compose --profile default --profile test-e2e up -d --build backend-core ai-engine browser-engine web-ui

      - name: Wait for services to be healthy
        run: |
          for url in http://localhost:8080/health http://localhost:8090/health http://localhost:8083/health http://localhost:8089/_stcore/health; do
            timeout 120 bash -c "until curl -fsS $url > /dev/null 2>&1; do sleep 2; done"
            echo "ready: $url"
          done

      - name: Build test image
        run: |
          docker compose --profile default --profile test-e2e build test-e2e

      - name: Run E2E tests
        run: |
          docker compose --profile default --profile test-e2e run --rm --no-deps test-e2e

      - name: Copy test results
        if: always()
        run: |
          mkdir -p test-results
          CONTAINER_ID=$(docker compose --profile default --profile test-e2e ps -q test-e2e)
          if [ -n "$CONTAINER_ID" ]; then
            docker cp $CONTAINER_ID:/app/test-results/. ./test-results/ || true
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: test-results/

      - name: Show service logs
        if: failure()
        run: |
          docker compose --profile default --profile test-e2e logs backend-core
          docker compose --profile default --profile test-e2e logs ai-engine
          docker compose --profile default --profile test-e2e logs browser-engine
          docker compose --profile default --profile test-e2e logs web-ui

      - name: Cleanup
        if: always()
        run: docker compose --profile default --profile test-e2e down -v

  # ==================== PERFORMANCE TESTS ====================
  performance-tests:
    name: Performance Tests
    runs-on: ubuntu-latest
    needs: integration-tests
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Start infrastructure
        run: |
          docker compose up -d ollama
          sleep 15

      - name: Start services
        run: |
          docker compose up -d --build backend-core ai-engine browser-engine
          sleep 20

      - name: Install load testing tools
        run: |
          pip install locust httpx

      - name: Run load tests
        run: |
          # Basic load test with curl
          for i in {1..100}; do
            curl -s http://localhost:8080/health > /dev/null &
          done
          wait
          echo "Load test completed"

      - name: Cleanup
        if: always()
        run: docker compose down -v

  # ==================== CODE QUALITY ====================
  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          pip install flake8 pylint black==26.3.1 isort==6.1.0 bandit safety

      - name: Run flake8
        run: |
          flake8 services/ --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 services/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Run pylint
        run: |
          find services/ -name "*.py" | xargs pylint --exit-zero

      - name: Check code formatting with black
        run: |
          black --check services/

      - name: Check import sorting with isort
        run: |
          isort --check-only services/

      # `mypy services/` cannot work here (three services own a package named
      # `app`); scripts/type-check.sh is the same gate `make type-check` runs,
      # and it pins its own mypy + stub versions. No `|| true`: this used to
      # swallow the result of a command that had never analysed a single line,
      # so the step was green for two independent reasons at once.
      - name: Run mypy type checking
        run: ./scripts/type-check.sh

      - name: Run bandit security checks
        run: |
          bandit -r services/ -f json -o bandit-report.json || true

      - name: Upload security report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: bandit-report.json

  # ==================== TEST COVERAGE REPORT ====================
  coverage-report:
    name: Generate Coverage Report
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    if: always()
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all coverage artifacts
        uses: actions/download-artifact@v4
        with:
          path: coverage-artifacts

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install coverage tools
        run: |
          pip install coverage pytest-cov

      - name: Combine coverage reports
        run: |
          mkdir -p combined-coverage
          find coverage-artifacts -name "*.xml" -exec cp {} combined-coverage/ \;
          
      - name: Generate combined report
        run: |
          echo "# Test Coverage Summary" > coverage-summary.md
          echo "" >> coverage-summary.md
          echo "## Service Coverage" >> coverage-summary.md
          for file in combined-coverage/*.xml; do
            service=$(basename $file .xml)
            echo "- $service" >> coverage-summary.md
          done

      - name: Upload combined coverage
        uses: actions/upload-artifact@v4
        with:
          name: combined-coverage-report
          path: |
            combined-coverage/
            coverage-summary.md

      - name: Comment PR with coverage
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const summary = fs.readFileSync('coverage-summary.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: summary
            });

  # ==================== BUILD AND PUSH IMAGES ====================
  build-images:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [unit-tests, security-tests, integration-tests, code-quality]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    strategy:
      matrix:
        service:
          - backend-core
          - ai-engine
          - browser-engine
          - web-ui
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/tab-organizer-${{ matrix.service }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=semver,pattern={{version}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./services/${{ matrix.service }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ==================== DEPLOYMENT ====================
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build-images
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    environment:
      name: staging
      url: https://staging.example.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to staging
        run: |
          echo "Deploying to staging environment..."
          # Add deployment commands here

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build-images, e2e-tests, performance-tests]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://production.example.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 

      - name: Deploy to production
        run: |
          echo "Deploying to production environment..."
          # Add deployment commands here

  # ==================== NOTIFICATION ====================
  notify:
    name: Send Notifications
    runs-on: ubuntu-latest
    needs: [unit-tests, security-tests, integration-tests, e2e-tests, code-quality]
    if: always()

    steps:
      - name: Check job status
        id: check
        run: |
          if [ "${{ needs.unit-tests.result }}" == "failure" ] || \
             [ "${{ needs.security-tests.result }}" == "failure" ] || \
             [ "${{ needs.integration-tests.result }}" == "failure" ] || \
             [ "${{ needs.e2e-tests.result }}" == "failure" ] || \
             [ "${{ needs.code-quality.result }}" == "failure" ]; then
            echo "status=failure" >> $GITHUB_OUTPUT
          else
            echo "status=success" >> $GITHUB_OUTPUT
          fi

      - name: Send notification
        run: |
          echo "Pipeline status: ${{ steps.check.outputs.status }}"
          # Add notification logic here (Slack, email, etc.)
