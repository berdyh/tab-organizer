pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-timeout==2.3.1
httpx==0.26.0
requests==2.32.3
fastapi==0.109.0
# Pinned to match all three services/*/requirements.txt. Boot mode (T6) launches
# a service as a real subprocess to regain per-test env control against any
# language; proving that mechanism against THIS stack means the test image has
# to be able to start the Python services the same way production does.
uvicorn[standard]==0.27.0
pydantic==2.5.3
jinja2==3.1.3
PyYAML==6.0.2
numpy==1.26.3
pandas==2.2.0
# Pinned to match services/ai-engine/requirements.txt. Without these the
# clustering pipeline's ImportError fallbacks take over and the unit suite
# characterises SVD + k-means while claiming to test UMAP + HDBSCAN -- which
# is what it did until 2026-08-07. `test_clustering.py` now asserts the real
# backend ran, so dropping either line fails the build instead of silently
# swapping the algorithm under the tests.
umap-learn==0.5.5
hdbscan==0.8.33
# NOT optional and NOT merely transitive. umap-learn 0.5.5 calls
# `check_array(force_all_finite=...)`, which scikit-learn renamed in 1.6, so an
# unpinned resolve floats sklearn forward and UMAP dies with
# `TypeError: check_array() got an unexpected keyword argument 'force_all_finite'`.
# Pinned to the exact version services/ai-engine/requirements.txt uses so the
# test image runs the same numeric stack as production.
scikit-learn==1.4.0
lancedb==0.6.13
pyarrow==15.0.0
cryptography==42.0.0
