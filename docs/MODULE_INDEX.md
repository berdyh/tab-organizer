# Module Index

> NOTE 2026-07-24: the TS migration plan (docs/ARCHITECTURE_PLAN.md) supersedes the
> service-topology boundary rules here; cards remain authoritative for day-to-day Python
> work until cutover. The "preserve the four runtime services" rule is explicitly
> superseded (plan decision 22).

This index is the top-level context for module/submodule work. Load this file
first, then the relevant local `MODULE.md`, then only the source/tests linked by
that card.

## Module Tree

| Module | Local card | Purpose | Main connections |
| --- | --- | --- | --- |
| Backend Core | `services/backend-core/MODULE.md` | API gateway, sessions, URL/tab state, agent tab APIs, exports, platform accounts, orchestration | Web UI, AI Engine, Browser Engine, Ops Tooling |
| AI Engine | `services/ai-engine/MODULE.md` | Provider runtime, embeddings, chunked RAG, clustering, local CLI providers | Backend Core, Browser Engine, shared config, LanceDB/Ollama |
| Browser Engine | `services/browser-engine/MODULE.md` | HTTP/browser scraping, CDP tab harvesting, auth detection, callbacks, downstream indexing | Backend Core, AI Engine, Ops Tooling |
| Web UI | `services/web-ui/MODULE.md` | Streamlit app, page state, user workflows, API facade | Backend Core, AI Engine, Browser Engine |
| Ops Tooling | `scripts/MODULE.md` | CLI lifecycle, host AI mode, tab CLI/MCP wrappers, init, Docker/test orchestration | Docker Compose, tests, all services |
| Shared Config | `config/MODULE.md` | AI model/provider catalog and config loader | AI Engine, CLI/init, Web UI settings |
| Test Harness | `tests/MODULE.md` | Unit, integration, E2E, load, and contract checks | All modules |

## Current Boundary Rules

- Preserve the four runtime services and Docker ports; this rework is not a
  service split or Git submodule conversion.
- Keep underscore compatibility packages such as `services/ai_engine` and
  `services/backend_core`; tests and CLI imports depend on them.
- Make behavior changes only after module-local tests exist for the affected
  boundary.
- Keep service contracts stable unless the relevant module card and connected
  cards are updated in the same change.
- Treat local CLI provider execution, bearer tokens, maintainer signup, API
  tokens, scrape callbacks, and browser lifecycle as security-sensitive areas.
- Protect Backend Core agent tab APIs with `BACKEND_AGENT_API_TOKEN`; do not
  route agent browser-control calls through unauthenticated paths.
- Browser Engine calls back only to Backend Core (`POST /api/v1/ingest/v1`);
  Backend Core is the single ai-engine `/index` writer. Capture (browser-engine
  today, the TS capture process later) never writes vectors directly.
- Keep CDP tab harvesting attach-only and local-endpoint-only until a browser
  lifecycle module explicitly owns profile launch/cleanup behavior.

## Validation Ladder

Use the smallest relevant check first:

1. Module target such as `make test-backend`, `make test-ai`,
   `make test-browser`, `make test-web`, or `make test-ops`.
2. Cross-service smoke: `./scripts/cli.py test --type integration`.
3. Full gate: `./scripts/cli.py test --type all`.
4. Browser/manual smoke when UI behavior or scraping behavior changed.

Rungs 2 and 3 are not optional for the single ingest endpoint. `POST
/api/v1/ingest/v1` is the wk10 cutover unblocker, and its replay/ordering/auth
rules and the durable index-forward outbox only mean anything over real HTTP
against real SQLite: `tests/integration/test_api.py::TestIngestV1` and
`tests/e2e/test_workflow.py::TestCaptureIngestWorkflow` are the in-situ
coverage (added for gap G3), and a change to `ingest.py`,
`SessionManager.ingest_scrape_result`, or the callback auth dependency must run
both. Note that a degraded ai-engine (no embedding provider chosen — the
fail-closed default) is the CORRECT bed for most of that suite: the forward to
`/index` genuinely fails, which is exactly the condition the outbox exists for.
The one test that needs a real embedding provider is marked `requires_ollama` /
`requires_lancedb` and skips with the reason ai-engine reported.

## Stub And Cleanup Ledger

| Item | Classification | Owner module | Decision |
| --- | --- | --- | --- |
| Legacy skipped gateway E2E file | remove | Test Harness | Removed; current direct-service coverage lives in `tests/e2e/test_workflow.py`. |
| Old Web UI Node/Jest scripts | remove | Web UI | Removed obsolete Docker/debug scripts; retained wrappers call repo-level pytest targets. |
| Backend fire-and-forget scrape trigger errors | replaced | Backend Core / Browser Engine | `POST /scrape` dispatch failures now surface as HTTP 502 and mark URLs `failed` (commit 3f1dcd8); inline `urls` are registered via `add_urls_to_session` before dispatch so callbacks persist instead of failing "URL not found" (WI0 B3). Covered by `tests/unit/test_scrape_callback.py`, `tests/unit/test_backend_callback_persistence.py`. |
| Browser callback best-effort failures | replace-with-contract | Browser Engine | Keep best-effort behavior explicit in the card and tests. Browser-engine now delivers each result to backend `POST /api/v1/ingest/v1` and never writes vectors; a 200-`ignored` (duplicate/stale) is a successful delivery, only transport/4xx/5xx increment `backend_callback_failed` (one document per ingest). Covered by `tests/unit/test_browser_engine_callbacks.py`. |
| Inline `/scrape` urls lost before registration (WI0 B3) | replaced | Backend Core | `start_scraping` calls `session_manager.add_urls_to_session(session.id, urls)` before dispatching inline urls (dedupes against the existing store), so each scrape-complete callback finds its record and writes the url_record + FTS row. Covered by `tests/unit/test_backend_callback_persistence.py::test_inline_scrape_urls_*`. |
| Hybrid search 500 on dead semantic leg (WI0 B4) | replaced | Backend Core | `search_tabs` catches a failing `_semantic_search`; hybrid mode degrades to keyword hits plus a `degraded: "semantic_unavailable: <reason>"` field, semantic-only mode raises HTTP 502 with `{code, cause, fix}` detail. `search.semantic_degraded` logged via `log_event`. Covered by `tests/unit/test_scrape_callback.py::test_*_search_*`. |
| Batch `/index` counters under-report failure (WI0 B5) | replaced | Backend Core / Browser Engine | Structurally fixed by the single-writer ingest: browser-engine no longer batches `/index` (that code + the `ai_index` accounting are deleted). Backend-core forwards per applied capture, so `ai_index_failed`/`ai_index_pending` are inherently per-document; they live in the `ingest_captures` ledger and `GET /api/v1/scrape/status/{id}` overlays them (with per-url `downstream_errors`) onto the browser payload, merging not replacing. Covered by `tests/unit/test_ingest_v1.py::test_scrape_status_overlays_index_failures`. |
| Single versioned idempotent ingest endpoint (WI0 B3/B5 root, Codex clobber) | replaced | Backend Core / Browser Engine | `POST /api/v1/ingest/v1` (`services/backend-core/app/api/ingest.py`, `IngestResultV1`) is the sole content writer: `capture_id`+`attempt`+`fetched_at` give replay protection (`duplicate`) and newest-wins ordering (`stale`), both acknowledged 200-`ignored` so correct rejection is never miscounted. `SessionManager.ingest_scrape_result` applies ledger row + url_record + FTS in one transaction under the `RLock`; in-memory and SQLite paths are parity-tested. Backend-core is the single ai-engine `/index` writer (async forward, per-key lock + latest-applied `superseded` guard). The legacy `POST /api/v1/callback/scrape-complete` is a deprecated shim over the same path (`attempt=0`, synthetic capture_id, unchanged response, `ingest.legacy_callback_deprecated`). Browser-engine switched `on_result` to `/ingest/v1` in the same change. **Round-2 hardening (Codex adversarial review, findings manager.py:688/735/706/932/1102, ingest.py:58/177, routes.py:1275):** (688) `ingest_scrape_result`'s SQLite path runs `BEGIN IMMEDIATE` before the duplicate/ordering reads so check-then-write is serialized even across two managers on one DB; (735) duplicate replay reads ONLY `capture_id` — session/normalized/auth_used/content come from the STORED ledger row + record (`build_forward_document`), a mutated redelivery can't clobber/forward the wrong body; (58) `IngestResultV1.fetched_at` is validated (422 on garbage) + normalized to canonical UTC-naive microsecond ISO, and all ordering flows through one parse-based `capture_order_key(source_rank, parsed-UTC fetched_at, attempt)` (no lexicographic compare survives; garbage → `datetime.min` loses forever); (1275) legacy is its own `source_rank=0` tier that loses to every v1 capture regardless of clock skew; (177) durable index-forward outbox — the ledger IS the outbox, `reconcile_pending_forwards` sweeps it opportunistically (per-ingest, pending-only) and at backend-core startup (all sessions, pending+failed) to recover crash-orphaned forwards, plus a post-lock ledger-state re-check guards double-forward; (706) `_ingest_apply` snapshots + restores in-memory state on a failed SQLite write so memory and DB stay consistent; (932) `capture_index_counts` reports only the latest applied capture per url; (1102) in-memory keyword search filters exactly like the FTS insertion condition (scraped-only, title+content haystack). Covered by `tests/unit/test_ingest_v1.py` (incl. `test_two_writer_race_older_write_cannot_clobber`, `test_duplicate_replay_forwards_stored_capture_not_mutated_body`, `test_ingest_v1_rejects_malformed_fetched_at_422`, `test_fetched_at_offsets_normalized_and_compared_chronologically`, `test_legacy_capture_never_outranks_v1_regardless_of_clock`, `test_startup_reconcile_forwards_orphaned_pending`, `test_opportunistic_sweep_recovers_pending_on_next_ingest`, `test_reconcile_marks_non_latest_superseded`, `test_forward_skips_when_already_indexed_after_lock`, `test_index_counts_reflect_only_latest_applied_capture`, `test_keyword_search_parity_pending_and_url_only_matches_excluded`, `test_forced_write_failure_keeps_memory_and_sqlite_consistent`), `tests/unit/test_browser_engine_callbacks.py`, `tests/unit/test_backend_callback_persistence.py`. **In-situ coverage (gap G3, closed):** every claim above was unit-only until now — the endpoint had never run against a live stack. `tests/integration/test_api.py::TestIngestV1` (13 cases) and `tests/e2e/test_workflow.py::TestCaptureIngestWorkflow` now exercise it over real HTTP against real SQLite: applied/duplicate/stale, a mutated redelivery that must not overwrite the stored body, newest-wins in both arrival orders, 404/409, 422 at the wire boundary, token-scope isolation against the tokens the stack was actually started with (ai-engine and browser-engine tokens → 401, callback token → 200 asserted first so the probe cannot pass vacuously), the ledger row settling out of `pending` after a genuinely failing forward and staying addressable, 8 concurrent identical captures → exactly one `applied`, a racing older/newer pair → newer content wins, and capture → keyword-searchable → retry → recapture. Each was verified by breaking the behaviour and observing the failure (replay protection off → concurrent captures 500; duplicate path rewriting the record → the mutated body appears in `/export`; ordering check off → `applied` instead of `stale_capture` and the record title flips to `Older`; callback auth widened to the other scopes → "the ai service token was accepted"; the failed-forward state update dropped → "ledger row never left 'pending'"; unregistered-url 409 removed → 200; `fetched_at`/`attempt`/`status` validators removed → 200 instead of 422; `should_forward` forced False → `skipped` instead of `pending`; the url_record/FTS write skipped in the ingest transaction → keyword search returns nothing). Still NOT covered in situ: `reconcile_pending_forwards`' STARTUP sweep (needs a backend-core restart the in-container runner cannot drive; the per-ingest opportunistic sweep and the duplicate-receipt retry are covered), a count of how many `/index` POSTs ai-engine actually received (not observable over HTTP — the ledger overlay that would show it is unreachable, see the `/scrape/status` row below), and the capture→vector→semantic-hit leg (needs a real embedding provider; marked `requires_ollama`/`requires_lancedb`, skips with the reason ai-engine announced, and was proven non-vacuous by forcing the gate open — it then failed on `state='failed'`). |
| `GET /api/v1/scrape/status/{id}` bound to the wrong function (FOUND, NOT FIXED) | stub | Backend Core | `routes.py`'s `@router.get("/scrape/status/{session_id}")` decorates the private helper `_overlay_ingest_status(session_id, payload)` instead of `get_scrape_status` (introduced in a3d685f, the ingest commit). FastAPI therefore treats `payload: dict` as a required request BODY on a GET: every real caller gets `422 {"loc": ["body"], "msg": "Field required"}` — verified live against the running stack — including `services/web-ui/src/pages/scraping.py` via `SyncAPIClient.get_scrape_status()`. `get_scrape_status` is dead code, so the browser-engine proxy and the finding-28 honest `status: "unknown"` behavior never run, and the B5 `ai_index_failed`/`ai_index_pending`/`downstream_errors` overlay is unreachable over HTTP. No test caught it because `tests/unit/test_ingest_v1.py::test_scrape_status_overlays_index_failures` calls `routes._overlay_ingest_status(...)` directly rather than through the app. Left unfixed deliberately: a production fix is a different concern from the G3 test wave, and it needs its own regression test asserting the route answers a GET with no body. |
| CDP attach impossible in Docker (WI0 B2) | partially-replaced | Browser Engine | Interim: `resolve_cdp_connect_url` resolves the input host (e.g. `host.docker.internal`, which Chrome's debug port rejects as a Host header) to its IP before connecting, keeping `validate_cdp_url`'s allowlist for INPUT; connect failures raise `CDPConnectionError` with a `{code, cause, fix}` message naming the two real requirements (Chrome reachable from Docker; host-side `socat` bridge). Durable fix is host-side capture in the TS migration (plan decision 24). Covered by `tests/unit/test_browser_tab_harvester.py`. |
| Backend platform store monolith | isolate-for-later | Backend Core platform | Split by auth/tokens/companies/dashboard/issues in a behavior-preserving pass. |
| Web platform page monolith | isolate-for-later | Web UI pages | Split render sections after page-level tests are green. |
| Concept-diagram `ichrome` tab harvester | replace-with-contract | Browser Engine tabs | V1 contract is Playwright CDP attach through `CDPTabHarvester`; add `ichrome` only behind the tabs card and tests. |
| Browser launch/profile management | isolate-for-later | Browser Engine tabs | Current implementation attaches to a user-started local Chrome/Chromium and never closes the profile. |
| Textual TUI and rofi/fzf quick-pick | isolate-for-later | Ops Tooling | CLI/MCP wrappers are the active agent surface; richer local navigation UIs should stay behind `scripts/` boundaries. |
| Full MCP SDK server package | replace-with-contract | Ops Tooling | Current contract is Python-callable wrappers plus JSON-lines stdio adapter; promote to SDK server only with MCP validation. |
| BERTopic topic modeling | isolate-for-later | AI Engine clustering | Current cluster engine remains UMAP + HDBSCAN + LLM labels; add topic modeling behind the clustering submodule. |
| Throwaway credential-store key | replaced | Browser Engine auth | Fail-closed: OS keyring or `CREDENTIAL_ENCRYPTION_KEY`, else `store()` raises `CredentialStoreError`; never invents a key. Covered by `tests/unit/test_credential_store.py`. |
| Unbounded cluster recursion | replaced | AI Engine clustering | `cluster()` bounded by `max_subcluster_depth` + no-progress guard. Covered by `tests/unit/test_clustering.py`. |
| Small-N clustering 500 (WI0 B9) | replaced | AI Engine clustering | UMAP params keep `n_components + 1 < n_samples`; below `min_cluster_corpus` a single "All Tabs" cluster is returned. Covered by `tests/unit/test_clustering.py`. |
| No runtime logging / request IDs (finding 29, WI0 B8) | replaced | Backend Core / AI Engine / Browser Engine | Shared `services/observability.py` gives stdlib JSON-lines logging + `RequestIDMiddleware` (accept/echo `X-Request-ID`) and outbound propagation on all inter-service httpx calls. Copied into all three service images (Dockerfiles updated; ai-engine newly copies the `services/` package). Seams logged: scrape dispatch, per-callback success/failure, index batch counts, provider switch, cluster failure, auth-queue events — never tokens/credentials/page bodies. Covered by `tests/unit/test_observability.py`. web-ui excluded. |
| Frozen security-invariant suite (SECSUITE 1.3.0) | replace-with-contract | Test Harness | Black-box probes SEC-1..42 in `tests/security/` freeze URL safety, CDP local-only, token scopes/fail-closed auth, credential isolation, agent subprocess hardening, prompt envelope, CI secret scanning (`.gitleaks.toml` + `secret-scan` job), and WI0-B6 auth fixtures. FROZEN: edits need a ledger row here + a `SECSUITE_VERSION` bump. Excludes `/platform/*` (decision 41). Challenge fixtures xfail until the B6 fix deletes the marker. **1.1.0**: added the `backend_ingest_v1` door to the token-scope matrix (`POST /api/v1/ingest/v1` unauth/foreign-principal → 401, callback token allowed) alongside the single-ingest-endpoint change. **1.2.0** (T7, closing two self-blind spots where probes existed but could never fail the build): added SEC-40/41 (`test_prompt_envelope.py`, `sec_seam`) as the hermetic, GATING prompt-assembly-seam probes for the untrusted-content envelope -- they drive `RAGChatbot.chat()` / `TabClusterer.generate_cluster_label()` directly against the same poisoned fixtures SEC-34/35 use, through a real CLI provider pointed at the hermetic `agent_cli_recorder` stub, needing no live embedder; SEC-34/35 are now documented as live-stack certification probes only (`sec_managed` + an embedding-backend skip stacked them so they could never fail CI alone). Reworked SEC-25 to assert the observed agent-subprocess env against a frozen data fixture (`fixtures/agent_env_allowlist.json`) instead of importing `AgentCLILLMProvider.ENV_ALLOWLIST`, so it is genuinely black-box; added SEC-42 (`sec_seam`) as the drift check pinning that Python constant against the same fixture. `tests/security/README.md`'s seam-exceptions section updated (SEC-25 removed as an exception, SEC-40/41/42 added). Verified live: broke the CLI prompt-assembly ordering seam and observed SEC-40/41 fail, then reverted; shrank the fixture allowlist and observed SEC-25 and SEC-42 both fail, then reverted. **1.3.0** (batch A of the wk0 security remediation, PROSE-ONLY edit — no assertion in `tests/security/` was added, removed, weakened or otherwise changed): `test_token_scopes.py`'s module docstring and the `browser_auth_pending` door comment previously described browser-engine's cross-scope accept fallback as an intentional documented tradeoff and told readers not to "fix" it. That fallback has now been removed (see the token-scope-collapse row below), so the prose was rewritten to record the removal, why the tradeoff framing was wrong, and that the door is now load-bearing in the real deployed configuration rather than only under the harness's synthetic per-scope tokens. `tests/security/MODULE.md`'s per-scope-token rule updated to match. The version bump is required by the suite's own change rules even though the edit is documentation-only. |
| Never-called `validate_config()` + silent unusable-provider health (finding 27, WI0 B1) | replaced | Backend Core / AI Engine / Browser Engine / Shared Config | Each service's FastAPI `lifespan` now calls `config.config_loader.get_ai_config().validate_config()` and refuses to start (raises `RuntimeError`, logs `config.invalid`) on a malformed `ai_models.yaml`. Backend Core and Browser Engine Dockerfiles/`requirements.txt` now carry `config/` + `PyYAML` to support this (previously ai-engine only). ai-engine's lifespan additionally checks `llm_client.get_runtime_health()`; an unusable selected LLM/embedding provider logs a structured `provider.unusable_at_startup` event and reports `GET /health` as `{"status": "degraded", "runtime": {...reason...}}` — it never crashes the service (degraded-visible beats dead). Covered by `tests/unit/test_startup_config_validation.py`. |
| Config divergence: `.env.example` vs compose/yaml defaults (finding 30) | replaced | Shared Config | Originally resolved by converging `.env.example` `AI_PROVIDER`/`EMBEDDING_PROVIDER` on `openrouter` to match `docker-compose.yml` and `ai_models.yaml`. **Superseded by the provider-routing row below**: converging on a default was the wrong resolution — the divergence was a symptom of there being a default at all. All three now agree on *no value*. (This row also claimed `EMBEDDING_PROVIDER=openrouter` "could only ever fail because OpenRouter serves no embedding models" — false, corrected 2026-08-05; openrouter serves six embedding models. The resolution stands on the no-default argument alone.) `ai_models.yaml`'s ollama `base_url: http://localhost:11434` is commented as the host-process default; containers always override via `OLLAMA_HOST` (docker-compose passthrough already existed). |
| Implicit provider selection + silent embedding fallback (`docs/SPEC-provider-routing.md` R1/R2/R4) | replaced | AI Engine / Shared Config | `AI_PROVIDER`/`EMBEDDING_PROVIDER` have no default in `llm_client.py`, `docker-compose.yml` (`${AI_PROVIDER:-}`) or `.env.example`; the embedding path no longer swaps in a capable provider when the chosen one cannot embed. Both raise `ProviderSelectionError` (`{code, cause, fix}`), which `LLMClient.__init__` stores rather than raises so the service starts `degraded` instead of going dark; request paths fail closed. Capable-provider lists are catalog-derived (`is_provider_supported`), never hardcoded. R4 server surfaces added: `provider.active` log line per role at startup, and a top-level `providers` block on `GET /health` beside `runtime` (`runtime` is diagnostics; `providers` is the stable attribution contract). R3 is an invariant stated in `LLMClient.__init__`'s docstring, not a mechanism — the general router stays TS. R4's UI badge (plan decision 16) and R5 per-response attribution (wk10 cutover) are deliberately not built in Python. Covered by `tests/unit/test_provider_routing.py` (now wired into `make test-ai`, which never ran it). An adversarial review executed five ways past the first implementation, all fixed in `b1fe195`: `/cluster` absorbed the selection error and returned invented labels with HTTP 200; a typo'd `EMBEDDING_MODEL` still killed the service at import because the fix had matched the *provider* example rather than the class; unauthenticated `/health` returned URL userinfo verbatim (so the invariant is no key, token, **or credentialed URL**); `EMBEDDING_DIMENSIONS` drift was announced rather than refused; and `LLMClient.PROVIDERS` had no test at all. The wave's own claim that "each gate was verified to fail" was false when written — see the warning in `docs/SPEC-provider-routing.md`. |
| Silent status synthesis on browser-engine outage (finding 28) | replaced | Backend Core / Web UI | `GET /api/v1/scrape/status/{id}` (routes.py) and `SyncAPIClient.get_scrape_status()` (web-ui client.py) no longer fabricate `not_started`/`completed` from local URL-store counts when browser-engine 404s, errors, or is unreachable — both return an explicit `{"status": "unknown", "detail": "status unavailable: ..."}` (backend still includes local counts as best-effort reference data). `scraping.py` renders the new status. Covered by `tests/unit/test_scrape_callback.py`, `tests/unit/test_web_ui_text.py`. |
| Hardcoded inter-service URL literals + boolean-only health (finding 32, 33) | replaced | Backend Core / Web UI | `routes.py` and `client.py` each resolve `AI_ENGINE_URL`/`BROWSER_ENGINE_URL`/`BACKEND_URL` through a single named `DEFAULT_*` constant per URL instead of an inline literal (web-ui builds from its own Docker context and cannot import routes.py's constants directly, so the two constant sets are kept in step by convention, not by import). `SyncAPIClient.check_health()` now passes through each service's real status string (`backend_status`/`ai_engine_status`/`browser_engine_status`, including `http_error:<code>`/`unreachable`) instead of collapsing to booleans only; `settings.py` renders it. Covered by `tests/unit/test_scrape_callback.py::test_backend_service_urls_use_runtime_env`, `tests/unit/test_web_ui_text.py`. |
| Web UI bypasses Backend Core for chat (WI0 B7) | replaced | Backend Core / Web UI | New `POST /api/v1/chat` in `routes.py` proxies to AI Engine `/chat` (mirrors the `/cluster` proxy pattern), attaching the AI Engine bearer token server-side. `web-ui/src/api/client.py`'s `chat()` now calls Backend Core, not `ai_url`, directly. Web UI's `search()`/`summarize_session()` still call ai-engine directly — a known, untouched remaining gap (not fixed in this change). Covered by `tests/unit/test_web_ui_platform.py::test_ai_client_request_shapes_include_shared_token`, `tests/unit/test_web_ui_text.py`. |
| Dead `services/web-ui/.env.example` (finding 31) | removed | Web UI | Deleted; held React/Node vars (`REACT_APP_API_URL`, `NODE_ENV`) the Streamlit app never reads. |
| No `make smoke-test` target (finding 34) | replaced | Test Harness | Added `smoke-test` target running `pytest -m smoke`; `tests/unit/test_startup_config_validation.py::test_lifespan_starts_cleanly_on_valid_config` is now marked `@pytest.mark.smoke`. |
| Unauthenticated `POST /api/v1/chat` proxy (wk0 review P1) | replaced | Backend Core | `/chat` (WI0 B7 proxy to AI Engine) now requires `Depends(_require_backend_agent_auth)`, matching `/search` and the agent tab routes; previously any LAN client on :8080 could spend AI Engine provider quota unauthenticated. Known follow-up gap, not fixed here: Web UI's `chat()` client sends no bearer token and `docker-compose.yml`'s `web-ui` service is not granted `BACKEND_AGENT_API_TOKEN`, so the Streamlit chat page 401s until that wiring lands. |
| `tests/security` not run by CI/test-unit (wk0 review P1) | replaced | Test Harness | Added a standalone `security-tests` CI job (`.github/workflows/ci-cd.yml`) running `pytest tests/security -m "security and not integration" -q` inside the `test-unit` profile image, kept separate from `tests/unit`'s default command per the existing `make test-backend`/`test-ai`/... pattern; wired into `build-images`' `needs` so it gates the build. Added `make test-security` for local parity. No `tests/security/` file received a semantic edit (comments/docs only), so no `SECSUITE_VERSION` bump. |
| CORS outermost swallows X-Request-ID on preflight (wk0 review P2) | replaced | Backend Core / AI Engine / Browser Engine | Starlette wraps middleware in reverse add-order (last added = outermost); all three services' `app/main.py` now add `RequestIDMiddleware` after `CORSMiddleware` so it wraps CORS and stamps `X-Request-ID` even on an OPTIONS preflight CORS answers directly. Covered by `tests/unit/test_observability.py::test_cors_preflight_still_carries_request_id`. |
| CDP resolved host not checked local (wk0 review P2) | replaced | Browser Engine | `resolve_cdp_connect_url` now rejects any resolved IP that is not `ipaddress`-private/loopback (`CDPConnectionError(code="cdp_resolved_address_not_local")`) before Playwright connects, closing a poisoned/misconfigured-resolver path to a non-local CDP target. `tests/security/test_cdp_local_only.py`'s existing SEC-11..15 assertions are unaffected (all exercised hosts stay in `CHROME_HOST_HEADER_SAFE`, which skips resolution); the new check is covered at the unit layer (`tests/unit/test_browser_tab_harvester.py::test_resolve_cdp_connect_url_rejects_non_local_resolved_address`), consistent with the existing CDP-fix ledger row, since a DNS-poisoning probe isn't expressible as a black-box HTTP request without becoming a third frozen-suite seam exception. |
| Token-scope fallback mistaken for a bug + SEC-25 seam undocumented (wk0 review P2, honesty fix) | documentation-only | Test Harness | `tests/security/test_token_scopes.py` now cites CLAUDE.md's documented browser-token-fallback invariant inline (module docstring + `browser_auth_pending` door comment) so it isn't "fixed" by a future reader; `services/browser-engine/MODULE.md` states the accepted blast-radius tradeoff (callback-only token also reaches scrape/CDP-control). `tests/security/README.md` gained a "Seam exceptions" section listing SEC-26/SEC-39 (already `sec_seam`) and SEC-25 (`AgentCLILLMProvider.ENV_ALLOWLIST` import, not yet markable -- no black-box process-introspection primitive and no second implementation to port against; revisit when the TS Agent SDK adapter lands), with a matching inline comment in `test_credential_isolation.py`. No assertions changed. |
| Collapsed service-token scopes: agent token opened the browser control plane (wk0 batch A, item 1) | replaced | Ops Tooling / Backend Core / Browser Engine | `scripts/cli.py`'s `service_env_with_tokens()` minted ONE `data/host-ai-token` value and assigned it to `AI_ENGINE_API_TOKEN`, `BACKEND_CALLBACK_TOKEN` and `BACKEND_AGENT_API_TOKEN`, never setting `BROWSER_ENGINE_API_TOKEN`; browser-engine's `_browser_engine_token()` accepted `BROWSER_ENGINE_API_TOKEN` -> `BACKEND_CALLBACK_TOKEN` -> `AI_ENGINE_API_TOKEN`. Net effect in every stock deployment: the agent token WAS the token browser-engine accepted, so an agent principal opened the whole credential + CDP control plane (verified by execution: agent token alone returned 200 on `/auth/pending`, `/auth/expire`, `/scrape`, `/detect-auth` and passed auth on `/auth/credentials`, `/tabs/import`). Fixed as one coupled change: (a) `ensure_service_token(env_name)` mints one INDEPENDENT `secrets.token_urlsafe(32)` per scope for all four of `SERVICE_TOKEN_ENVS`, persisted in `data/service-tokens.json` (0600) so tokens stay stable across restarts, never clobbering an operator-supplied env/`.env` value, and seeding `AI_ENGINE_API_TOKEN` (only) from the legacy `data/host-ai-token` file so existing installs do not rotate silently; `cmd_host_ai` and `run_backend_tab_tool` now ask for their own scope. (b) backend-core's `_browser_engine_headers()` sends `BROWSER_ENGINE_API_TOKEN` only -- all six backend->browser call sites (scrape dispatch, tab import, tab open, scrape status, auth pending, auth credentials) route through the single `_browser_engine_request_headers()` builder. (c) browser-engine's `_browser_engine_token()` accepts `BROWSER_ENGINE_API_TOKEN` only and fails CLOSED (401) when unset. OUTBOUND token-selection fallbacks are deliberately untouched (`_service_token_headers("BACKEND_CALLBACK_TOKEN", "AI_ENGINE_API_TOKEN")`, backend's ai-headers builder): they choose which token to SEND, not which to ACCEPT. Covered by `tests/unit/test_cli_host_ai.py` (`test_service_env_mints_four_distinct_tokens_in_stock_env`, `test_service_env_seeds_ai_token_from_legacy_single_token_file`, `test_service_env_never_clobbers_user_supplied_tokens`), `tests/unit/test_browser_engine_callbacks.py::test_browser_engine_auth_requires_browser_scope_token`, `tests/unit/test_scrape_callback.py::test_browser_engine_headers_send_only_browser_scope`, and the frozen `browser_auth_pending` token-scope door. The stock-env guard is a UNIT test on purpose: `tests/security/conftest.py` overwrites all four token vars with distinct values, so a probe there could never observe the collapse. |
| Unauthenticated backend credential proxies (C3, wk0 batch A item 2) | replaced | Backend Core | `GET /api/v1/auth/pending` and `POST /api/v1/auth/credentials` had no auth dependency while attaching the privileged Browser Engine service token server-side -- a confused deputy letting any unauthenticated caller on :8080 read the pending-auth queue and plant credentials for an arbitrary domain. Both now take `Depends(_require_backend_agent_auth)`, matching `/search` and `/chat`. Covered by `tests/unit/test_scrape_callback.py::test_backend_auth_proxy_requires_agent_token`. Known follow-up gap (Web UI batch, not fixed here): `services/web-ui/src/api/client.py`'s `get_pending_auth()`/`submit_credentials()` send no bearer token and the `web-ui` compose service is not granted `BACKEND_AGENT_API_TOKEN`, so those Streamlit flows 401 until that wiring lands -- the same gap already recorded for `chat()`. |
| Wildcard CORS + credentials on all three services (wk0 batch B, item 1) | replaced | Backend Core / AI Engine / Browser Engine | All three `app/main.py` files ran `allow_origins=["*"]` WITH `allow_credentials=True`. Verified live before the fix: a preflight claiming `Origin: https://evil.example` returned 200 echoing that origin with `access-control-allow-credentials: true`. Chained with backend-core's unauthenticated reads, any page the user had open while the stack ran could `fetch('http://localhost:8080/api/v1/sessions')` then `/api/v1/urls/{id}` and pull back stored page CONTENT, including tabs imported from the user's logged-in browser session. Adjudication claim CONFIRMED by reading the code: `services/web-ui/src/api/client.py` uses the `requests` library server-side (Streamlit calls backend/ai/browser from the container), and the only browser-facing port is Streamlit's :8089 -- none of the three services has a legitimate browser-side caller. Fixed by a new shared `services/cors.py` (`allowed_origins()`, env `CORS_ALLOWED_ORIGINS`, default `http://localhost:8089` + `http://127.0.0.1:8089`) wired into all three services with `allow_credentials=False`; the middleware was NARROWED rather than removed so the allowlist is explicit, a future browser-side Streamlit component still works, and the existing `RequestIDMiddleware`-wraps-CORS ordering contract keeps a real preflight to exercise. All three Dockerfiles now `COPY services/cors.py`. `tests/unit/test_observability.py::test_cors_preflight_still_carries_request_id` switched to an allowed origin (a foreign origin now gets Starlette's 400 "Disallowed CORS origin" preflight). Frozen by `tests/security/test_cors_policy.py` (SEC-43). |
| `/urls` listing returned full captured page content (wk0 batch B, item 2) | replaced | Backend Core | `GET /api/v1/urls/{session_id}` returned `r.metadata` verbatim and `SessionManager` writes `metadata["content"] = capture.content`, so the unauthenticated list endpoint handed back the full text of every captured page -- the payload half of the CORS exfiltration chain above. Now returns an explicit whitelist projection (`URL_LIST_METADATA_FIELDS = title, status_code, auth_type, auth_used, capture_id`) via `_project_url_metadata`; unknown metadata keys are dropped deny-by-default so a future capture field cannot leak by accident, and page text stays exclusive to `/export`. Adjudication's consumer check CONFIRMED: `services/web-ui/src/pages/url_input.py:161` reads only `url_data["status"]` and `url_data["original"]`; `tests/e2e/test_workflow.py:94-101` asserts only `len(records)` and `record["original"]`/`record["status"]`; `tests/integration/test_api.py:219` reads content from browser-engine `/scrape/single` and `tests/e2e/test_workflow.py:181-182` from `/export`, neither from this endpoint. Frozen by `tests/security/test_route_exposure.py` (SEC-45). |
| Published ports reachable from the whole LAN (wk0 batch B, item 3) | replaced | Ops Tooling | `docker-compose.yml` published 8080/8083/8089/8090 (and Ollama's 11434) on all interfaces, so any device on the network could reach the unauthenticated backend reads and the Streamlit UI. Every `ports:` entry now binds `127.0.0.1`. Adjudication claim CONFIRMED: the `test-integration`/`test-e2e` services set `BACKEND_URL=http://backend-core:8080` etc. (docker-network service names), never the published mapping, and every inter-service default in code is a service-name URL -- so integration/E2E are unaffected; `scripts/cli.py`'s health checks target `http://localhost:<port>/health`, which loopback still serves. Ollama's port was bound too for consistency (host-run `--host-ai` reaches it over loopback). CLAUDE.md gained a "Published ports are loopback-only" invariant explaining the binding and how to deliberately expose one service for remote access. Covered by `tests/unit/test_runtime_auth_config.py::test_published_ports_are_bound_to_loopback`. |
| Web UI 401s on newly-authenticated backend routes (wk0 batch B, item 4) | replaced | Web UI / Ops Tooling | Commits 08b6ea5 (`/chat`) and 0a22621 (`/auth/pending`, `/auth/credentials`) added `Depends(_require_backend_agent_auth)` to three backend routes web-ui calls, but the `web-ui` compose service was never granted `BACKEND_AGENT_API_TOKEN` -- all three flows 401 against a real stack. `docker-compose.yml` now passes `BACKEND_AGENT_API_TOKEN` to `web-ui`, and `SyncAPIClient` reads it in `__init__` and sends it from exactly those three methods via a new `_backend_agent_headers()`. Not a scope violation: web-ui already held `AI_ENGINE_API_TOKEN`, Streamlit runs server-side so the token never reaches a browser, and the agent scope is strictly weaker than the browser scope. A full grep of `services/web-ui/` found no other call to an authenticated backend route (the only other outbound calls are ai-engine's `/search`, `/summarize`, `/providers`, `/providers/switch`, `/config` -- already token-carrying -- plus tokenless `/health` probes and the platform endpoints, which use the user's own session JWT). Adjudication claim CONFIRMED and acted on: `BROWSER_ENGINE_API_TOKEN` was granted to `web-ui` but read nowhere in `services/web-ui/` (its only browser-engine call is the tokenless `/health` probe), so it was REMOVED from the service env, reducing blast radius. Out of scope and untouched: `search()` still bypasses backend to call ai-engine directly. Covered by `tests/unit/test_web_ui_platform.py::test_backend_agent_scoped_calls_carry_the_agent_token`, `::test_ai_client_request_shapes_include_shared_token`, `tests/unit/test_runtime_auth_config.py::test_web_ui_service_holds_only_the_tokens_it_uses`. |
| Frozen security-invariant suite (SECSUITE 1.4.0) | replace-with-contract | Test Harness | **1.4.0** (wk0 batch B item 5) adds three black-box probes for self-blind spots the suite had: SEC-43 (`test_cors_policy.py`) -- no probe issued an `Origin` header anywhere, so wildcard CORS was invisible; it asserts a foreign origin is neither echoed nor wildcarded and `access-control-allow-credentials` is never true on all three services, plus a `sec_managed` non-vacuity check that the configured UI origin IS granted (so a removed middleware fails too). SEC-44 (`test_route_exposure.py`) -- the table-driven probe that would have caught the unauthenticated credential proxies: it enumerates the route table from the service's own `/openapi.json` (not a hand-maintained list, so a NEW route is unclassified and fails until someone decides), requires 401 for anonymous callers on everything not on the commented `PUBLIC_ROUTES` allowlist, and separately checks that allowlist for staleness and for still being genuinely anonymous-reachable so it cannot become a rubber stamp. SEC-45 (same file) -- ingests a capture with a known body via `/api/v1/ingest/v1`, then asserts the `/urls` listing carries neither that text, a `content` key, nor any unreviewed metadata key. All three verified by deliberate breakage: reinstating `allow_origins=["*"]`/`allow_credentials=True` failed all 9 SEC-43 probes; deleting the two `Depends(_require_backend_agent_auth)` failed SEC-44 naming `GET /api/v1/auth/pending -> 200` and `POST /api/v1/auth/credentials -> 422`; restoring `"metadata": r.metadata` failed SEC-45 on the captured page text. All restored green. |
| Credentials followed cross-host redirects (C7, sec-followup) | replaced | Browser Engine | `_scrape_basic_auth`/`_scrape_cookie_auth` attached the user's stored credentials at the httpx CLIENT level, and `_safe_httpx_request`'s manual redirect loop reused that client for up to 10 cross-host hops -- so an open redirect or a compromised credentialed origin handed the `Authorization` header and session cookie to an arbitrary host. Reachability traced: the credential-store path is LATENT in a stock install (`CREDENTIAL_ENCRYPTION_KEY` ships blank, `keyring` is in no requirements file, so `CredentialStore.store()` fails closed and `POST /auth/credentials` 503s), but the `_safe_browser_route_handler` variant -- ambient browser-session cookies forwarded verbatim from a `use_browser=True` scrape -- WAS default-install reachable. Fixed in three verified rounds, each round refuted by an adversarial reviewer executing the attack against the previous fix: (1) credentials moved to per-request kwargs with a sticky never-re-armed drop once a hop leaves the credential origin, counting scheme downgrade and port change as crossings but not an `http->https` upgrade; (2) `client.cookies.clear()` per hop, because the jar is populated from every `Set-Cookie` and keyed on the resolved IP literal (the DNS-rebinding defense), so any two hostnames sharing a CDN edge or load balancer leaked -- and the fix's own `Cookie`-header strip was what freed the jar to inject; plus `credential_scope_drop` metadata, a WARNING-level `scrape.credentials_dropped_on_redirect` event, and `auth_used=False` on a dropped chain so a logged-out capture cannot be indexed as authenticated; (3) the `{authorization, cookie}` deny-list replaced by a four-entry transport allowlist (`accept`, `accept-encoding`, `accept-language`, `user-agent`), since a 38-header sweep showed `x-api-key`, `x-csrf-token`, `if-none-match`, `proxy-authorization` and every vendor auth header crossing freely -- with no shared-IP precondition. Round 3 also closed a leak found outside its mandate: `Referer` carried the full secret URL of a credentialed origin to third-party subresource hosts with no redirect involved, now reduced to origin (dropped on downgrade). CRLF/NUL in a cookie name or value now refuses the request at a single choke point rather than emitting a second header line. Strict host comparison KEPT and its rationale recorded: the credential store holds a bare `{name: value}` dict with no domain metadata, so `example.com -> www.example.com` legitimately drops -- relaxing to registrable domain needs a public-suffix list this repo does not carry, and domain metadata in the store is the real fix (stub recorded on the scraper card). Covered by `tests/unit/test_scraper_redirect_credentials.py` (18 tests, every assertion proven to gate by deliberate revert). |
| CDP "local-only" gate accepted routable addresses (C6, sec-followup) | replaced | Browser Engine | `resolve_cdp_connect_url` gated on `is_private or is_loopback`, which Python's `ipaddress` reports TRUE for cloud-metadata `169.254.169.254`, benchmark `198.18.0.0/15`, `192.0.0.0/24`, documentation `2001:db8::/32`, and -- the sharpest case -- 6to4 `2002::/16`, where `2002:808:808::` encodes global IPv4 `8.8.8.8`. A poisoned resolver or hostile `/etc/hosts` entry for `host.docker.internal` therefore passed a check whose entire purpose is local-only, and Playwright was pointed at the target. Round 1 bolted a four-item denylist on top and was refuted for closing instances rather than the class; round 2 inverted to an explicit `LOCAL_CDP_NETWORKS` allowlist (loopback, RFC1918 `10/8` + `172.16/12` + `192.168/16`, IPv6 ULA `fc00::/7` excluding link-local) with everything else refused by default. Re-verified against 32 encodings -- IPv4-mapped (`::ffff:8.8.8.8`), IPv4-compatible, NAT64 `64:ff9b::/96`, deprecated site-local `fec0::/10`, 6to4 encoding an RFC1918 target, CGNAT, TEST-NET, and exact range boundaries -- with no bypass found, and `172.17.0.1` (docker bridge) plus `127.0.0.1` still accepted. Two regressions the widening to `getaddrinfo` introduced were also closed: address selection is now deterministic and IPv4-preferred among allowlisted survivors (the card's own `socat ... bind=172.17.0.1` guidance is IPv4), and a dual-stack answer with one unsafe address filters rather than rejecting outright. IPv6 netloc bracketing fixed at both rebuild sites (`http://[::1]:9222` previously became `http://::1:9222`, reparsing with `hostname=None`). Covered by `tests/unit/test_browser_tab_harvester.py`. |
| Credential store documented as working while inoperative (C8, sec-followup) | replaced | Browser Engine | `queue.py` imports `keyring` inside `_load_key_from_keyring`, but `keyring` appears in no requirements file, so in every shipped image the import raises `ImportError` and resolution falls through to `CREDENTIAL_ENCRYPTION_KEY` -- which ships blank in `.env.example` and empty in `docker-compose.yml`. The store therefore fails closed and credential submission 503s in every default install: the auth-scraping feature does not function out of the box, while this ledger claimed the mitigation was "OS keyring or CREDENTIAL_ENCRYPTION_KEY". Scope decision: DOC-CORRECTION ONLY -- the dependency was deliberately not added and key-resolution logic was not touched, because shipping a working credential path is a product decision (it makes credential storage live by default) reserved for the maintainer. The prose now states what actually resolves, documents how to enable it (`Fernet.generate_key()` into `CREDENTIAL_ENCRYPTION_KEY`), and records two hazards on the auth card: enabling the store activates C7 unless that fix is present (it is, in this branch), and adding `keyring` later without pinning a backend would let `keyrings.alt`'s `PlaintextKeyring` silently persist the Fernet key in cleartext. |
| Subscription models routed as metered, and a catalog that hid it | replaced | Shared Config / AI Engine / Ops Tooling | `codex_cli.default_models.llm` was `gpt-5.6-luna` while `openrouter.default_models.llm` was `openai/gpt-5.6-luna` -- the same weights, two ids, two cost models, and nothing in the catalog connecting them. `defaults.use_cases.chat_metered` and `recommended: true` on `google/gemini-2.5-flash-lite` did the same for Gemini. The result: the metered copy of a model the user already pays for by subscription was the default on the openrouter path. **Representation chosen (2026-08-06): route-per-entry.** Each `models:` entry stays ONE provider route whose key is the exact wire id (it is written verbatim to `LLM_MODEL`/`EMBEDDING_MODEL`), and gains `model_family:` -- the logical identity shared by every route to the same weights. `cost_model` stays on the provider and is resolved per route by the new `get_model_cost_model()`; `describe_model()`, `get_family_routes()` and `format_model_description()` expose the route, and every menu in `configure-provider`/`init.py` renders through the last of these so a listing can never show a bare model name. **Rejected:** one merged entry per model with a per-provider id map -- it makes the key an abstract name needing translation in every adapter and pushes `dimensions` into a per-provider sub-map, a second way to resolve the wrong vector width (ollama `qwen3-embedding` 1024-d vs openrouter `qwen/qwen3-embedding-8b` 4096-d). **Also rejected:** copying `cost_model` onto model entries. Behaviour changes: `openrouter.default_models.llm` -> `qwen/qwen3.7-flash`; `use_cases.chat_metered` -> `deepseek/deepseek-v4-flash-0731`; `recommended` dropped from `openai/gpt-5.6-luna` and `google/gemini-2.5-flash-lite`; all four metered GPT-5.6/Gemini copies now carry `superseded_by:` naming their subscription route; new `use_cases.smoke_test` keeps openrouter as the deliberate 'is it wired up' path. The rule is data (`routing.metered_duplicate_policy: never_default`) and is executed by `test_no_metered_route_is_defaulted_when_the_family_has_a_subscription_route`, with `test_the_metered_duplicate_set_is_not_empty` as its non-vacuity guard; `validate_config()` now also requires `model_family` and resolves `superseded_by` to a real subscription route. Verified by mutation, each watched to fail and restored: openrouter default back to `openai/gpt-5.6-luna` -> "openrouter.default_models.llm = openai/gpt-5.6-luna (family 'gpt-5.6-luna' is served by a subscription provider)"; `recommended: true` back on the gemini copy -> same test; `model_family` deleted -> family-link test plus all four `test_startup_config_validation` lifespans; configure-provider's formatter reverted -> menu-row test; a metered provider added to `llm_preference_order` -> "gemini is in llm_preference_order but is 'metered'". **Two of the tests written for this change could not fail and were caught by running the mutation rather than asserting it:** the first dimensions test compared the catalog to itself and survived a width changed from 4096 to 1024 (replaced by `VERIFIED_EMBEDDING_DIMENSIONS`, pinned independently of the catalog, plus a coverage guard); a second survived a family-hoisting mutation because no family in the catalog has two embedding routes of different width (replaced by `test_two_routes_to_one_family_keep_their_own_vector_widths`, which builds that case in a synthetic catalog). |
| `gemini_cli` subscription provider (UNVERIFIED END-TO-END) | stub | AI Engine / Shared Config / Ops Tooling | Added so Gemini work spends the user's subscription instead of openrouter. **`antigravity` cannot be that provider** and was not used: `/usr/bin/antigravity` -> `/opt/Antigravity/antigravity` is an Electron GUI IDE with no headless mode -- `--version` opened a window with a DevTools listener, `--help` never returned. The `gemini` CLI is the headless substitute (`-p/--prompt`, `-m`, `--acp`), installed at `~/.local/bin/gemini` 0.54.0. `GeminiCliLLMProvider` follows the existing adapters: `local_cli`, `cost_model: subscription`, `GEMINI_CLI_COMMAND`, LLM-only (`embeddings: false` verified by RUNNING `gemini --help` -- its command list is mcp/extensions/skills/hooks/gemma/[query] and there is no embedding surface), in `routing.llm_preference_order`, wired into `PROVIDERS`, `CLI_PROVIDER_COMMANDS`, `_create_llm_provider`, `_cli_provider_available`, `host-ai`, `check-provider`, `.env.example` and `docker-compose.yml`. Hardening mirrors its siblings: `--approval-mode plan` clamped to `{plan, default}` so no env var reaches `yolo`; untrusted-scraped-context refused by default (gemini has NO tool-free mode -- its own bundled `policies/read-only.toml` allows `read_file`, `google_web_search`, `web_fetch`); `GEMINI_API_KEY`/`GOOGLE_API_KEY` deliberately absent from `ENV_ALLOWLIST` so the subscription route cannot silently become the metered one; envelope in argv with an in-process byte limit (stdin stays DEVNULL). **The measured hazard that shaped it:** logged out, `gemini --version` exits 0 while `gemini -p '...'` blocks FOREVER on "Opening authentication page in your browser ... [Y/n]" -- reproduced 2026-08-06 with stdin closed AND under `setsid`, surviving `timeout`, killed with SIGKILL. The inherited `--version` preflight would therefore have advertised the provider as available and hung every request to the timeout, so `_availability_preflight` also requires `~/.gemini/oauth_creds.json` (`GEMINI_DIR`/`OAUTH_FILE` read from the installed bundle). That is a NECESSARY condition, not proof of a live session, and it is a deliberate departure from this repo's call-the-endpoint rule because calling the endpoint IS the hazard here. **STUB, not done: no generation through this adapter has ever been observed.** The CLI on the development host is unauthenticated, so the four registered model ids come from the shipped bundle's own default-model constants rather than from a call, and `test_gemini_cli_generates_against_the_real_subscription` (`requires_provider_credentials`) SKIPS -- it can never pass in that state. Closing this needs one interactive `gemini` login and a rerun. Hermetic coverage is 11 tests in `tests/unit/test_subscription_cli_providers.py` plus SEC-46/47, all mutation-verified (credential check removed -> availability test fails; untrusted gate removed -> refusal test and SEC-47 fail; approval mode unclamped -> yolo test and SEC-46 fail; argv guard removed -> oversize test fails; error message put in the exception -> leak test fails). No `gemini_cli` route to family `gemini-2.5-flash-lite` is registered, because catalog keys are wire ids and the bare id belongs to the direct-API route; both metered copies point at `gemini-3.1-flash-lite` via `superseded_by` instead. |
| Frozen security-invariant suite (SECSUITE 1.5.0) | replace-with-contract | Test Harness | **1.5.0** adds SEC-46/47 (`test_agent_subprocess_hardening.py`, `sec_managed`) for the `gemini_cli` adapter, and a `"gemini"` output format to `AgentCLIRecorder.set_control`. Rationale, which generalises: every agent-subprocess probe NAMES its provider -- SEC-28/31/32/33 are claude-only, SEC-29/30 codex-only -- so a third CLI adapter fell entirely outside the frozen suite except for the shared env-allowlist checks (SEC-25/42, which stay green because the adapter needs no new env var). SEC-46 asserts the subprocess is headless (`-p`), read-only (`--approval-mode plan`), stdin-free, and that the guardrail preamble precedes the user block **in argv** (this adapter puts the envelope there, not on stdin, so SEC-31's ordering check does not reach it). SEC-47 asserts scraped content never reaches the CLI by default. Both stage `~/.gemini/oauth_creds.json` into a temp HOME and ASSERT the provider switch returned 200 rather than skipping on it -- without that they would skip silently and assert nothing, verified by unstaging the credentials and observing both fail with "gemini_cli was not selectable, so this probe would assert nothing: 400". Verified by mutation: defaulting the adapter to `yolo` failed SEC-46 (`assert 'yolo' == 'plan'`); deleting the untrusted-context gate failed SEC-47 (`assert 200 >= 500`). Both restored green. |
| Frozen security-invariant suite (SECSUITE 1.6.0) | replace-with-contract | Test Harness | **1.6.0** executes plan decision 44 (Addendum 2026-08-05), the hedge against deferring `SEC_BOOT_*_CMD` boot mode to ~wk8. Every `sec_managed` probe's INPUTS and EXPECTED REFUSALS moved out of Python and into language-neutral JSON: `fixtures/agent_subprocess_hardening.json` (SEC-28..33, 46, 47), `credential_isolation.json` (SEC-25, 27), `prompt_envelope.json` (SEC-34/35 plus the shared envelope contract SEC-40/41/36 now also read), `token_scope_failclosed.json` (SEC-21, 22), `cors_policy.json` (SEC-43), `url_safety_escape_hatch.json` (SEC-10) -- one file per probe FAMILY, following `agent_env_allowlist.json`'s shape (a `_comment` stating the contract, why it exists, and the change rules). Boot mode becomes a runner over this data when it lands, and softening a probe to fit whatever got built is now a reviewable fixture diff instead of a quiet edit in a test body. New `tests/security/contracts.py` holds the loader and assertion runners; they RAISE on an unrecognised or empty contract block and on `expect` keys a probe stops reading (`assert_expect_keys_consumed`), because a generic runner that silently skips what it does not understand is how a typo turns an assertion into a no-op. New SEC-48 (`test_fixture_completeness.py`, `sec_seam`) is the guard on the hedge: it derives the `sec_managed` list from the suite's own module/function markers and reconciles it against `fixtures/sec_managed_index.json` three ways (every probe registered, every entry resolving to a real non-empty contract block, no entry naming a dead probe), so probe N+1 added inline next year fails the build instead of silently falling outside the hedge. **No assertion was weakened** -- this changed WHERE each contract lives, not WHAT it says; the suite went 188 passed/3 skipped/5 deselected -> 205 passed/3 skipped/5 deselected, the +17 being SEC-48 and its 16 per-entry resolution cases. Verified by mutation in BOTH directions for every family, each observed failing: fixture side -- SEC-46 `--approval-mode plan`->`yolo` (`'--approval-mode' carried 'plan', contract requires 'yolo'`), SEC-27 `status_min 500`->`status 200` (`expected HTTP 200, got 500`), envelope ordering reversed (`'User request and retrieved content:' (at 765) must precede 'untrusted' (at 118)`), SEC-21[ai_embed] `401`->`200` (`expected HTTP 200, got 401`), SEC-43 non-vacuity origin -> the foreign origin (`backend did not grant its own configured UI origin 'https://evil.example'`), SEC-10 default `400`->`200` (`expected HTTP 200, got 400`), a deleted registry entry (`sec_managed probes with no fixture entry: [('test_url_safety_scrape', 'test_sec10_escape_hatch_default_off')]`); code side (applied to a throwaway copy of `services/`, never the worktree) -- claude `--tools ""`->`"all"` failed SEC-28, `ENV_ALLOWLIST` widened with `ANTHROPIC_API_KEY` failed SEC-25 (`agent env leaked non-allowlisted keys: {'ANTHROPIC_API_KEY'}`) and SEC-42, the guardrail preamble moved after the user block failed SEC-40/41 (`'untrusted' (at 149) must precede 'User request and retrieved content:' (at 0)`), ai-engine's unset-token 401 replaced by `return` failed SEC-21[ai_embed] and SEC-22[hatch_absent], `http://localhost:8089` dropped from the CORS default failed all three SEC-43 non-vacuity probes, `private_scrape_urls_allowed()`->`True` failed SEC-10[hatch_absent] (`expected HTTP 400, got 200`), and an unregistered inline `sec_managed` probe failed SEC-48. Also fixes a real doc drift found in passing: `README.md` line 3 said the suite was frozen at "1.4.0" while `__init__.py` said "1.5.0", and the attached-mode arithmetic ("175 of 194", "the 19 `sec_managed` probes") was never updated when SEC-46/47 landed. Real figures: 213 probes, 21 `sec_managed`, 192 attached-runnable. `docs/ARCHITECTURE_PLAN.md`'s Addendum 2026-08-05 carries the same stale pair ("173 of 192", "19") and still needs it. |
| OpenRouter recorded as embedding-incapable on evidence that could not see the answer | replaced | Shared Config / AI Engine / Ops Tooling | `config/ai_models.yaml` carried `openrouter.supports.embeddings: false` annotated **"VERIFIED FALSE 2026-08-04"**. It was false. The check behind it was real and honestly reported but structurally blind: it fetched `GET https://openrouter.ai/api/v1/models`, scanned all 340 entries, found output modalities `[audio, image, text]` and no id matching "embed", and inferred no embedding support -- but that listing enumerates the **chat-completions** surface only, and `POST /v1/embeddings` is a separate surface it never mentions. Verified by calling that endpoint directly on 2026-08-05: **six** embedding models serve 200 -- `qwen/qwen3-embedding-8b` (4096-d, $0.01/Mtok), `qwen/qwen3-embedding-4b` (2560-d, $0.02), `baai/bge-m3` (1024-d, $0.01), `google/gemini-embedding-001` (3072-d, $0.15), `openai/text-embedding-3-small` (1536-d, $0.02), `openai/text-embedding-3-large` (3072-d, $0.13); prices measured at two input sizes (~2250-3000 and ~2-5 tokens) that agreed exactly, ruling out single-token rounding. All six honour a `dimensions` override (768 requested -> 768 returned), so any can adopt the existing 768-wide `tab_organizer_docs` table with no reindex. The annotation is what made the error durable -- every later reader treated "verified" as settled and copied it onward into eleven artifacts. **Ratio that matters for the catalog-derived design:** with ONLY the catalog corrected and no code touched, `scripts/init.py` and `configure-provider`'s two embedding filters self-corrected (they ask `is_provider_supported`), and the pre-existing `test_provider_capability_mirror_agrees_with_the_catalog` immediately failed with `openrouter.embeddings disagrees with config/ai_models.yaml` -- it was NOT weaker than believed and needed no strengthening. Everything hardcoded needed hand repair: `LLMClient.PROVIDERS`' mirror (`ec4cc0a`), the `openrouter -> OpenAIEmbeddingProvider` adapter entry deleted as "unreachable" (`b1fe195`, wrong on both counts -- unreachable only because the capability check read a wrong flag, and the endpoint is OpenAI-compatible), a user-facing `fix` string, and six documents. New: `dimensions_configurable` on a model entry means the model accepts a `dimensions` parameter and emits exactly that width; `EmbeddingConfig.dimensions_configurable` carries it and `OpenAIEmbeddingProvider.embed` sends it, without which a 4096-d default against a 768-d table is a guaranteed refusal. `_resolve_embedding_dimensions` now accepts an override below the native width for such models (`embedding_dimensions_out_of_range` above it) while the fixed-width equality rule is unchanged. Covered by `tests/unit/test_provider_routing.py`: `test_embedding_capability_flag_agrees_with_the_adapter_map` (+ a non-staleness guard on its one documented exception, `deepseek` -- catalog says false, adapter exists, unresolvable without a `DEEPSEEK_API_KEY` since api.deepseek.com 401s before routing, so recorded as debt rather than guessed), `test_adapter_sends_dimensions_for_a_configurable_model` / `..._omits_dimensions_for_a_fixed_width_model` (hermetic, gate in CI), and two `requires_provider_credentials` live tests -- one per registered model asserting it serves AND truncates, one end-to-end through `LLMClient`. The `requires_provider_credentials` marker is new in `pytest.ini`: a real billed call, skipping (never passing) without a key, because the original error was a false belief about a remote service and no hermetic test can refute that. `tests/unit/test_cli_configure_provider.py`'s two "cannot embed" tests kept their mechanism and moved their example to `claude_code` (structurally incapable -- no Claude embedding adapter exists), guarded by `test_the_cannot_embed_example_is_genuinely_incapable`, since the old example silently stopped being valid while every test stayed green. |
| Provider routing: no silent selection, always announce (SPECIFIED, NOT IMPLEMENTED) | stub | AI Engine / Ops Tooling | `config/ai_models.yaml` now carries the contract as data (`routing:` block: `llm_preference_order` prefers the subscription CLIs, `silent_fallback: false`, `explicit_opt_in_required` covers ollama and every metered provider, `announce_active_provider: true`) but NO code enforces it yet. Today `llm_client.py:115` and `:141` hardcode `"openrouter"` as the default for both roles -- an unset env var silently spends money -- and `:148-152` silently rewrites the embedding provider when the chosen one cannot embed. Nothing announces which provider answered. Full behavioural spec, including which parts belong in Python now versus TS at the facade/cutover (per plan decision 21, never build it twice): `docs/SPEC-provider-routing.md`. Build in Python now: fail-closed selection (R1/R2), startup + `/health` announcement (R4), and `cli.py configure-provider` (R6, tooling that survives the migration). Defer to TS: the UI badge and per-response provider attribution. |
